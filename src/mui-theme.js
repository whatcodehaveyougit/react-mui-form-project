import { createTheme } from '@mui/material/styles';


const theme = createTheme({
    palette: {
      primary: {
        main: "#64B790",
        contrastText: "#FFFFFF",

      },
      secondary: {
        main: "#6174AD",
      },
      background: {
        default: "#6173AF"
      },
    }
  })

  export default theme;