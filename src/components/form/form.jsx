import { Container } from "@mui/system";
import { Button } from '@mui/material/';


const Form = () => {
    return (
        <Container>
            <form>
                <Button
                    size="large"
                    color="primary"
                    variant="contained"
                >Button</Button>

            </form>
        </Container>

    )
}

export default Form;